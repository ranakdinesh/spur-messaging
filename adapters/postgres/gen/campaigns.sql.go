// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: campaigns.sql

package gen

import (
	"context"
	"encoding/json"
	"time"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createCampaign = `-- name: CreateCampaign :one

INSERT INTO messaging.campaigns (
    tenant_id, name, channel, template_id, template_params, segment_id, contact_ids, scheduled_at, status
) VALUES (
    $1, $2, $3, $4, $5, $6, $7, $8, $9
) RETURNING id, tenant_id, name, channel, template_id, template_params, segment_id, contact_ids, scheduled_at, started_at, completed_at, status, stats, created_at, updated_at
`

type CreateCampaignParams struct {
	TenantID       uuid.UUID          `json:"tenant_id"`
	Name           string             `json:"name"`
	Channel        string             `json:"channel"`
	TemplateID     uuid.UUID          `json:"template_id"`
	TemplateParams json.RawMessage    `json:"template_params"`
	SegmentID      pgtype.UUID        `json:"segment_id"`
	ContactIds     []uuid.UUID        `json:"contact_ids"`
	ScheduledAt    pgtype.Timestamptz `json:"scheduled_at"`
	Status         string             `json:"status"`
}

// sql/queries/campaigns.sql
func (q *Queries) CreateCampaign(ctx context.Context, arg CreateCampaignParams) (MessagingCampaign, error) {
	row := q.db.QueryRow(ctx, createCampaign,
		arg.TenantID,
		arg.Name,
		arg.Channel,
		arg.TemplateID,
		arg.TemplateParams,
		arg.SegmentID,
		arg.ContactIds,
		arg.ScheduledAt,
		arg.Status,
	)
	var i MessagingCampaign
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Channel,
		&i.TemplateID,
		&i.TemplateParams,
		&i.SegmentID,
		&i.ContactIds,
		&i.ScheduledAt,
		&i.StartedAt,
		&i.CompletedAt,
		&i.Status,
		&i.Stats,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const deleteCampaign = `-- name: DeleteCampaign :exec
DELETE FROM messaging.campaigns
WHERE tenant_id = $1 AND id = $2
`

type DeleteCampaignParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
}

func (q *Queries) DeleteCampaign(ctx context.Context, arg DeleteCampaignParams) error {
	_, err := q.db.Exec(ctx, deleteCampaign, arg.TenantID, arg.ID)
	return err
}

const getCampaignByID = `-- name: GetCampaignByID :one
SELECT id, tenant_id, name, channel, template_id, template_params, segment_id, contact_ids, scheduled_at, started_at, completed_at, status, stats, created_at, updated_at FROM messaging.campaigns
WHERE tenant_id = $1 AND id = $2
`

type GetCampaignByIDParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
}

func (q *Queries) GetCampaignByID(ctx context.Context, arg GetCampaignByIDParams) (MessagingCampaign, error) {
	row := q.db.QueryRow(ctx, getCampaignByID, arg.TenantID, arg.ID)
	var i MessagingCampaign
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Channel,
		&i.TemplateID,
		&i.TemplateParams,
		&i.SegmentID,
		&i.ContactIds,
		&i.ScheduledAt,
		&i.StartedAt,
		&i.CompletedAt,
		&i.Status,
		&i.Stats,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const getScheduledCampaigns = `-- name: GetScheduledCampaigns :many
SELECT id, tenant_id, name, channel, template_id, template_params, segment_id, contact_ids, scheduled_at, started_at, completed_at, status, stats, created_at, updated_at FROM messaging.campaigns
WHERE status = 'scheduled' AND scheduled_at <= $1
`

func (q *Queries) GetScheduledCampaigns(ctx context.Context, scheduledAt pgtype.Timestamptz) ([]MessagingCampaign, error) {
	rows, err := q.db.Query(ctx, getScheduledCampaigns, scheduledAt)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []MessagingCampaign
	for rows.Next() {
		var i MessagingCampaign
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.Channel,
			&i.TemplateID,
			&i.TemplateParams,
			&i.SegmentID,
			&i.ContactIds,
			&i.ScheduledAt,
			&i.StartedAt,
			&i.CompletedAt,
			&i.Status,
			&i.Stats,
			&i.CreatedAt,
			&i.UpdatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listCampaigns = `-- name: ListCampaigns :many
SELECT id, tenant_id, name, channel, template_id, template_params, segment_id, contact_ids, scheduled_at, started_at, completed_at, status, stats, created_at, updated_at, count(*) OVER() as total_count
FROM messaging.campaigns
WHERE tenant_id = $1
AND ($2::text IS NULL OR status = $2)
ORDER BY created_at DESC
LIMIT $3 OFFSET $4
`

type ListCampaignsParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	Column2  string    `json:"column_2"`
	Limit    int32     `json:"limit"`
	Offset   int32     `json:"offset"`
}

type ListCampaignsRow struct {
	ID             uuid.UUID          `json:"id"`
	TenantID       uuid.UUID          `json:"tenant_id"`
	Name           string             `json:"name"`
	Channel        string             `json:"channel"`
	TemplateID     uuid.UUID          `json:"template_id"`
	TemplateParams json.RawMessage    `json:"template_params"`
	SegmentID      pgtype.UUID        `json:"segment_id"`
	ContactIds     []uuid.UUID        `json:"contact_ids"`
	ScheduledAt    pgtype.Timestamptz `json:"scheduled_at"`
	StartedAt      pgtype.Timestamptz `json:"started_at"`
	CompletedAt    pgtype.Timestamptz `json:"completed_at"`
	Status         string             `json:"status"`
	Stats          json.RawMessage    `json:"stats"`
	CreatedAt      time.Time          `json:"created_at"`
	UpdatedAt      time.Time          `json:"updated_at"`
	TotalCount     int64              `json:"total_count"`
}

func (q *Queries) ListCampaigns(ctx context.Context, arg ListCampaignsParams) ([]ListCampaignsRow, error) {
	rows, err := q.db.Query(ctx, listCampaigns,
		arg.TenantID,
		arg.Column2,
		arg.Limit,
		arg.Offset,
	)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ListCampaignsRow
	for rows.Next() {
		var i ListCampaignsRow
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.Name,
			&i.Channel,
			&i.TemplateID,
			&i.TemplateParams,
			&i.SegmentID,
			&i.ContactIds,
			&i.ScheduledAt,
			&i.StartedAt,
			&i.CompletedAt,
			&i.Status,
			&i.Stats,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.TotalCount,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateCampaign = `-- name: UpdateCampaign :one
UPDATE messaging.campaigns
SET name = $3,
    template_id = $4,
    template_params = $5,
    segment_id = $6,
    contact_ids = $7,
    scheduled_at = $8,
    status = $9,
    updated_at = now()
WHERE tenant_id = $1 AND id = $2
RETURNING id, tenant_id, name, channel, template_id, template_params, segment_id, contact_ids, scheduled_at, started_at, completed_at, status, stats, created_at, updated_at
`

type UpdateCampaignParams struct {
	TenantID       uuid.UUID          `json:"tenant_id"`
	ID             uuid.UUID          `json:"id"`
	Name           string             `json:"name"`
	TemplateID     uuid.UUID          `json:"template_id"`
	TemplateParams json.RawMessage    `json:"template_params"`
	SegmentID      pgtype.UUID        `json:"segment_id"`
	ContactIds     []uuid.UUID        `json:"contact_ids"`
	ScheduledAt    pgtype.Timestamptz `json:"scheduled_at"`
	Status         string             `json:"status"`
}

func (q *Queries) UpdateCampaign(ctx context.Context, arg UpdateCampaignParams) (MessagingCampaign, error) {
	row := q.db.QueryRow(ctx, updateCampaign,
		arg.TenantID,
		arg.ID,
		arg.Name,
		arg.TemplateID,
		arg.TemplateParams,
		arg.SegmentID,
		arg.ContactIds,
		arg.ScheduledAt,
		arg.Status,
	)
	var i MessagingCampaign
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.Name,
		&i.Channel,
		&i.TemplateID,
		&i.TemplateParams,
		&i.SegmentID,
		&i.ContactIds,
		&i.ScheduledAt,
		&i.StartedAt,
		&i.CompletedAt,
		&i.Status,
		&i.Stats,
		&i.CreatedAt,
		&i.UpdatedAt,
	)
	return i, err
}

const updateCampaignStats = `-- name: UpdateCampaignStats :exec
UPDATE messaging.campaigns
SET stats = $3,
    updated_at = now()
WHERE tenant_id = $1 AND id = $2
`

type UpdateCampaignStatsParams struct {
	TenantID uuid.UUID       `json:"tenant_id"`
	ID       uuid.UUID       `json:"id"`
	Stats    json.RawMessage `json:"stats"`
}

func (q *Queries) UpdateCampaignStats(ctx context.Context, arg UpdateCampaignStatsParams) error {
	_, err := q.db.Exec(ctx, updateCampaignStats, arg.TenantID, arg.ID, arg.Stats)
	return err
}

const updateCampaignStatus = `-- name: UpdateCampaignStatus :exec
UPDATE messaging.campaigns
SET status = $3,
    started_at = CASE WHEN $3 = 'running' AND started_at IS NULL THEN now() ELSE started_at END,
    completed_at = CASE WHEN $3 = 'completed' THEN now() ELSE completed_at END,
    updated_at = now()
WHERE tenant_id = $1 AND id = $2
`

type UpdateCampaignStatusParams struct {
	TenantID uuid.UUID `json:"tenant_id"`
	ID       uuid.UUID `json:"id"`
	Status   string    `json:"status"`
}

func (q *Queries) UpdateCampaignStatus(ctx context.Context, arg UpdateCampaignStatusParams) error {
	_, err := q.db.Exec(ctx, updateCampaignStatus, arg.TenantID, arg.ID, arg.Status)
	return err
}
